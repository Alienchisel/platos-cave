# Generated previews. All reproducible from tools/ -- ~11 MB of GIFs and contact
# sheets that would bloat history for no benefit, since every one of them is a
# build artefact of a script that is committed.
preview/

# Downloaded 3D scan (~13 MB). Free but login-walled, so it cannot be fetched
# automatically anyway. CC BY 4.0, Fitzwilliam Museum GR.23.1850 -- see README.
mesh/

# Source photographs and the renders derived from them. Large, and the baked
# assets in firmware/ are what the build actually consumes.
image/

# Python
__pycache__/
*.pyc
*.pyo

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# PlatformIO
.pio/
.pioenvs/
